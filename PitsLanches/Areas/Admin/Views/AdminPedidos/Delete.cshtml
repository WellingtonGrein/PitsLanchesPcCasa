@model PitsLanches.Models.Pedido
@{
    ViewData["Title"] = "Deletar Pedido";
}

<div class="admin-page-header">
    <h1>Deletar Pedido</h1>
</div>

<div class="admin-content-card">
    <div class="text-center">
        <i class="bi bi-exclamation-triangle-fill text-danger" style="font-size: 4rem;"></i>
        <h3 class="mt-3">Confirmação de Exclusão</h3>
        <p class="lead">Você tem certeza que deseja deletar permanentemente o pedido abaixo? <br /><strong>Esta ação não pode ser desfeita.</strong></p>
    </div>

    <hr />

    <div class="row justify-content-center">
        <div class="col-md-8">
            <dl class="row">
                <dt class="col-sm-4">Cliente</dt>
                <dd class="col-sm-8">@Model.Nome @Model.Sobrenome</dd>

                <dt class="col-sm-4">Número do Pedido</dt>
                <dd class="col-sm-8">#@Model.PedidoId</dd>

                <dt class="col-sm-4">Data do Pedido</dt>
                <dd class="col-sm-8">@Model.PedidoEnviado.ToString("dd/MM/yyyy HH:mm")</dd>
            </dl>
        </div>
    </div>

    <div class="admin-form-actions justify-content-center">
        <form asp-action="Delete">
            <input type="hidden" asp-for="PedidoId" />
            <input type="submit" value="Sim, Deletar Permanentemente" class="btn btn-danger btn-lg" />
            <a asp-action="Index" class="btn btn-secondary btn-lg">Cancelar</a>
        </form>
    </div>
</div>